describe("Filtrado en página de hoteles", () => {
    beforeEach(() => {
        cy.visit("www.paradise.com");
    });

    it("Verifica que todas las cards aparecen en la página desde el inicio", () => {
        cy.visit
    });

    it("Aplica cada filtro por precio y verifica que las cards que aparecen corresponden al filtro aplicado", () => {
        cy.get
    });

    it("Limpia los filtros y verifica nuevamente que todas las cards estén presentes", () => {
        cy.get 
    });
})